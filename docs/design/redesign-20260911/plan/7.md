# 배치7 계획표 — invest/[portfolioId] · invest/new · onboarding/household · transactions/[transactionId] (2026-09-13)

가설 = 배치1 pick **A + C** 확장(재생성 없음). 네 화면 모두 폼 — 배치3~6 폼 문법 그대로(필드 44 · 간격 14 · FormActions · 페이지 모드 = 좌 폼 · 우 FormGuide). 데이터는 백엔드 스키마 대조(`moeum-back/app/domain/{portfolio,transaction,household,account}`).
진행 = 사용자 "전체 총괄" 위임(2026-09-13) — 캔버스 ⏭, Figma 가 시안 · pick 은 추천안.

## 감사에서 나온 것 (코드 + 백엔드 대조)
- **종목 폼**(PortfolioSheet 정본, invest/new · invest/[portfolioId] 는 fallback): 간격 `sm` 8(다른 폼 14) · 조회 버튼 `variant="light"`(accent 틴트 채움 — §5 버튼은 filled/outline/subtle 뿐) · 설명이 인풋 위(다른 폼은 아래) · 페이지 모드 레일 비어 있음(주석 "이 폼 배치에서 문구와 함께").
- 종목 "삭제" = 백엔드 보관(`is_archived=True`, 보유 수량 > 0 이면 거절) — 확인 모달이 danger 색 없이 일반 확인(다른 폼 삭제는 danger).
- 종목 수정: 계좌·종목명 비활성(계좌 이동은 백엔드 불가 — `PortfolioUpdateRequest` 에 account_id 없음). 종목명은 백엔드 수정 가능하지만 조회로 채우는 값이라 비활성 유지.
- **거래 수정 페이지**(transactions/[transactionId], QuickAddSheet 정본 46:451 의 fallback): 폼은 배치2 완료, 레일 비어 있음. 잔액은 거래로 계산(`account/service._calc_balance`)이라 수정·삭제하면 통장 잔액이 다시 계산된다.
- **온보딩**: Card 1장 + Title 기본 크기 + 인풋 `size="md"` 반복 + 가운데 정렬 힌트(§1 가운데 정렬 없음) + `autoFocus`. 가입이 베타 차단이라 실제로는 관리자가 만든 계정만 거친다.
- 온보딩 중에도 셸(탭바·사이드바)이 보이고 누르면 온보딩으로 되돌아온다 — 새 사용자만 보는 화면이라 이번엔 보류로 적는다.

## 결정 (위임 — 추천안)
1. **조회 버튼** — **A(추천) outline 44**(필드와 한 줄, 보조 행동) / B subtle 글자 링크.
2. **종목 폼 설명 위치** — **A(추천) 인풋 아래**(다른 폼과 같게 `inputWrapperOrder`) / B 그대로.
3. **온보딩** — **A(추천) 로그인과 같은 한 칼럼**: 제목 22/800 → 설명 13 dim → 이름 필드 → 버튼 52 → 힌트 dim 13 좌정렬, 카드 0 / B 카드만 제거.

## invest/new · invest/[portfolioId] — 종목 폼
| 섹션 | 정보 | 컴포넌트 |
|---|---|---|
| ① 시트 | "종목 추가 / 종목 수정" · 데스크톱 모달 560 | FormSheet |
| ② 투자 계좌 | Select · 수정은 비활성 | Select |
| ③ 시장 | Select 5(코스피·코스닥·나스닥·NYSE·기타) · 설명 아래 | Select |
| ④ 종목코드 + 조회 | 필드와 "조회" outline 44 한 줄(기타면 숨김) | TextInput · Button outline |
| ⑤ 종목명 · ⑥ 현재가 | 설명 아래 · 현재가 단위 "원" | TextInput · NumberInput + InputUnit |
| ⑦ 푸터 | [삭제 좌측(수정 · 보유 0일 때만)] · 취소 · 저장 | FormActions |
| 페이지 모드 | 좌 폼 · 우 FormGuide: 시장(자동 시세 = 코스피·코스닥·나스닥·NYSE, 금·원자재는 기타) · 수량(추가 후 매수로 쌓여요) · 삭제(보유 0 일 때만 · 지난 매매 기록은 남음) | FormGuide |
- 간격 14. 삭제 확인 = danger.

## transactions/[transactionId] — 거래 수정 페이지
- 폼 = 배치2 QuickAddSheet 와 같은 TransactionForm. 레일 FormGuide: 유형(지출·수입·이체, 자산 통장은 평가 조정) · 잔액(기록·수정·삭제하면 통장 잔액이 다시 계산돼요) · 고정지출(고르면 그 항목의 이번 달 사용액에 더해져요).

## onboarding/household — 첫 가계부
| 섹션 | 정보 | 컴포넌트 |
|---|---|---|
| ① 제목 | "가계부에 오신 걸 환영해요" 22/800 · 설명 13 dim | Title · Text |
| ② 이름 | 가계부 이름 44 | TextInput |
| ③ 버튼 | "다음" filled 52 전체 폭 | Button lg |
| ④ 힌트 | "통화는 KRW(원), 시작일은 오늘로 정해져요" dim 13 좌정렬 | Text |

## 이번 배치에서 손대지 않는 것
온보딩 중 셸 숨김(보류) · 종목 계좌 이동(백엔드 없음) · 종목 보관 목록 UI(보관 = 삭제로만 씀).
