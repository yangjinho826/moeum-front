# QA redesign-20260911 배치1 — 2026-09-12 @미커밋(사용자 지시로 커밋 보류, S6 후 `/commit`)
토큰 신규 위반: 0(baseline 88 유지) · design-review(감사만, 픽스는 S5 코드에): blocking 0 / 수정 15 / 보류 8 → **보류 8건 전부 처리(2차, 사용자 지시)** · typecheck 0 · lint 0 · 콘솔 에러 0(4화면 × 390·1440)
외부 시각: Codex(소스 감사, HARD REJECTION = 계좌 상세 카드 잔존 → 배치2) + Claude 서브에이전트(일관성) — 겹친 것: 터치타깃·hero 자간·% 모노·StatGrid 잘림·달력 9px·스파크라인 키보드·reduced-motion
디자인 점수: 감사 전 B(hero 15px 렌더 = 시안 불일치 high, 터치 medium) → 수정 후 A- · AI slop A(3열 아이콘 그리드·그라데이션·카드 0)

## 대조표
| ID | handoff | shot | 차이 | 판정 |
|---|---|---|---|---|
| home | handoff/1/shots/Main.png · HomeDesktop.png | shots/home.jpg · home-desktop.jpg | hero 44/60 복구 후 일치. 모바일 shot 폭 500(크롬 최소) | ✅ |
| transactions | Transactions.png · TransactionsDesktop.png | shots/transactions.jpg · transactions-desktop.jpg | 데스크톱 = 목록 + 레일 달력(피드백 반영, handoff 와 의도적 차이) · 칩 38px | ✅ |
| invest | Invest.png · InvestDesktop.png | shots/invest.jpg · invest-desktop.jpg | 종목 비중 = B 구성 막대(Figma 25:179 갱신본과 일치, handoff 도넛과 의도적 차이) | ✅ |
| settings | Settings.png · SettingsDesktop.png | shots/settings.jpg · settings-desktop.jpg | 일치 | ✅ |

## 수정(S5 코드에 반영, 미커밋)
| # | 발견 | 심각도 | 수정 |
|---|---|---|---|
| F-001 | hero 금액 44/60 이 아니라 15px 로 렌더 — Mantine Text 단일클래스 규칙이 나중에 로드돼 `.moeum-hero-amount` 를 덮음 | high(시안 불일치) | globals.css 2클래스 선택자 `.moeum-mono.moeum-hero-amount(-compact)` |
| F-002 | Section 링크 13/500 · 높이 19px | medium | 12/600(DESIGN §5) + 패딩/음수 마진으로 32px 히트 |
| F-003 | FilterChip 27px | medium | 위 6/아래 5 패딩 → 38px |
| F-004 | 캡션 링크 "8월 기록" 16px · 투자 "종목 추가" 19px | medium | 32px 히트(패딩+음수 마진) |
| F-005 | 헤더 가계부 pill 28px | medium | 32px |
| F-006 | FAB 시각 40 = 터치 40 | medium | `.moeum-fab::before inset -4` → 48 |
| F-007 | hero letter-spacing 이 `.moeum-mono{letter-spacing:0}` 에 덮여 0 | medium | F-001 선택자에 포함 |
| F-008 | DeltaPill 수익률 % 가 Pretendard | low | `<span class="moeum-mono">` |
| F-009 | StatGrid 18px 금액이 390 3열(~106px)에서 잘림 | high(390) | `.moeum-mono.moeum-stat-value{clamp(13px,4vw,18px)}` lg 18 |
| F-010 | 달력 셀 금액 9px · 요일 10px | low | 10px(DESIGN 예외 기재) · 11px |
| F-011 | 투자 추이 X축 눈금 10px · 종점 3.5px | low | 11px · 3px |
| F-012 | 스파크라인 점 클릭만 가능(키보드·이름 없음) | high(a11y) | `role=button tabIndex aria-label(월) Enter/Space` + focus 강조, hero 가 월 라벨 전달 |
| F-013 | prefers-reduced-motion 미대응 | low | 전역 미디어 쿼리 |
| F-014 | UnstyledButton 행·FAB focus-visible 링 없음 | low | globals `:focus-visible` accent 2px |
| F-015 | DESIGN.md ↔ 코드/Figma 불일치(헤어라인 14 vs 12 · ListRow 14/600 vs 15/500) | doc | 승인된 Figma/shot 기준으로 DESIGN.md 정정 |

## 보류 → 2차 처리 (2026-09-12, "알아서 다 처리")
| # | 내용 | 처리 |
|---|---|---|
| D-1 | 계좌 상세 Card 3장 잔존 | Card 전부 제거: hero → `HeroAmount compact`+`DeltaPill`, 메타 → `StatGrid`, 자산 추이 → `Section`(툴팁 surface/hair, 전월 증감 up/down), 누적 매매수익 → `ListRow`, 보유 종목 → `Section`+`ListRow tall`. 정식 배치는 배치2 S3 그대로(shots/invest-account.jpg) |
| D-2 | "이유" 투자손익이 누적 값 | 월간 투자손익 API 없음 → 제목 방향은 hero 와 같은 "지난 기록 대비 총자산 증감"으로, 행은 "투자 평가손익 · 누적 · 평가 X · 수익률"로 정직하게. (decisions.md) |
| D-3 | 빈 상태 텍스트만 | `EmptyText`(문장 + accent 행동 링크) 공용 → 홈 최근 기록·거래 목록·달력·투자 빈 상태·보유 종목 5곳. 행동 = 기록하기(QuickAdd)/계좌 추가 |
| D-4 | 중복 코드 | `Hairline`·`PageTitle`·`EmptyText` 컴포넌트, `fmtSigned/fmtSignedPct/fmtArrowPct`(_utilities/fmt), `topExpenseCategories`(_features/stats/utils) 추출 → 헤어라인 4벌·타이틀 3벌·Top N 3벌·signed 2벌 제거. 인라인 fontSize 58건 토큰화는 **의도적으로 안 함** — QA 직후 30파일 스타일 diff 는 회귀 위험 > 이득, 배치2 S5 규칙("유틸 클래스 5종")으로 흡수 |
| D-5 | 세그먼트 규칙 모순 | DESIGN.md §5 를 "필터 = 칩(밑줄) / 화면 전환 = 세그먼트(채움 indicator)"로 분리 확정 |
| D-6 | recharts 초기 측정 warning | `ResponsiveContainer initialDimension` (Sparkline 320×44 · ValuationTrend 320×140) |
| D-7 | month-picker aria 영문 | `general.common.prev_month/next_month` ko·en |
| D-8 | 세그 전환 재확인 | 라벨 클릭으로 전환 확인(shots/transactions-calendar.jpg) — 자동화가 radio input 을 눌러서 안 됐던 것 |
검증: typecheck 0 · lint 0 · 토큰 신규 0 · 계좌 상세/거래/홈 렌더 · 콘솔 에러 0. 미삭제: `portfolio-donut.tsx`(참조 0 — 삭제는 사용자 확인 후). `design-tokens.ts` 는 color-picker·portfolio/utils·account/constants·login-section 이 아직 씀(배치2~3 에서 정리)

## 룰 구멍 메모
- S6 의 `/design-review` 는 clean tree + 픽스마다 커밋을 요구 → design-flow S5 게이트(커밋 해시)를 S6 앞에 강제하거나, "감사만 모드"를 build-qa.md 에 명시해야 함. 이번엔 사용자 결정으로 감사만.
- dev 서버 장기 실행(19일) 시 SSR 500(`useContext null`) 으로 두 화면 백지 → S6 시작 절차에 "dev 서버 재시작" 추가.
- 크롬 확장 스크린샷은 탭이 백그라운드면 Mantine 전환(rAF)이 멈춰 시트가 안 찍힘 → 상태는 DOM 으로 판정 병행.
- 렌더 shot 승인이 CSS 우선순위 버그(F-001)를 못 걸렀음 → S5 화면 규칙에 "hero·StatGrid 등 핵심 수치의 computed font-size 실측" 추가 제안.
