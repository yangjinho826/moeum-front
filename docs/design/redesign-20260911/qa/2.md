# QA redesign-20260911 배치2 — 2026-09-12 @29fa6e7(감사) · 픽스 38814e6 · 82ac282 (사용자 ✅)
토큰 신규 위반: 0(baseline 88 유지) · design-review(감사만 — clean tree 였지만 자동 커밋은 `/commit` 룰과 충돌, 배치1 과 같은 방식): **blocking 6 / 수정 17 / 보류 8** → 사용자 "전부" 결정, blocking 6 + 수정 17 처리 · 보류 중 H-1·H-7 결정 반영(나머지 6 은 문서 정정·배치3·기각) · typecheck 0 · lint 0 · 콘솔 에러 0(배치2 6경로 + 배치1 4화면 회귀, 390 iframe · 1440) · 가로 스크롤 0
외부 시각: Codex(소스 감사 15건) + Claude 서브에이전트(일관성 12건) — 겹친 것: 상태(로딩·에러·빈) 미구현 · 추이선 0 변화 빨강 · 아이콘 버튼 44 미만 · 배분 차트 모바일 높이 · 삭제 버튼 색 · 문구 톤
디자인 점수: 감사 전 B-(시안 불일치 3 · 상태 미구현) → 픽스 후 A-(남은 것 = 보류 8, 대비 토큰 H-1 이 가장 큼) · AI slop A(카드 0 · 그라데이션 0 · 3열 아이콘 그리드 0) · Litmus 7개 중 YES 6(카드 = 거래 라우트 fallback Card 1장, 배치3)

## 대조표
| ID | handoff | shot | 차이 | 판정 |
|---|---|---|---|---|
| invest/account | handoff/2/shots/Main.png · AccountDesktop.png | shots/invest-account.jpg · invest-account-desktop.jpg(재촬영) | 배치 일치. 종목 0 → 종목 비중 숨김(B-2 ✅) · 보유 종목 sub `▲ 17.0%` 로 평단 안 잘림(F-204 ✅) · 추이 -0.7% 완만(F-205 ✅) | ✅ |
| invest/portfolio | Stock.png · StockDesktop.png (정본 Figma 45:130) | shots/invest-portfolio.jpg · invest-portfolio-desktop.jpg(재촬영) | hero 라벨·매매 행 색 Figma 일치 · "매매 내역"(B-6 ✅) · 칩 34~51px(F-202 ✅) · 뒤로 44(제목 위치 52 그대로) · 데스크톱 레일 요약 = 매입금액만(의도적, API 에 종목별 실현손익 누적 없음) | ✅ |
| transactions/new | RecordSheet.png · RecordModalDesktop.png (정본 Figma 46:451) | shots/transactions-new.jpg(재촬영, 지출·이체) · 데스크톱 DOM 실측(560 · r12 · 가운데) | 시트가 탭바를 덮음(B-3 ✅, (100,820) = overlay) · 내용 높이로 줄고 푸터 바닥 16 위(F-217 ✅) · 다이얼로그 이름 "거래 추가" h2(F-201 ✅) · 금액 빈칸 + placeholder 0(F-215 ✅) · 세그 4개(의도적) · 금액 좌정렬 = Figma(H-2) · 통장 0개 안내(B-4 ✅, 코드) | ✅ |
| wealth | Wealth.png · WealthDesktop.png (정본 Figma 46:195) | shots/wealth.jpg · wealth-desktop.jpg(재촬영) | 자산군 고정 색 = handoff 와 같음(부동산 짙은색·현금 테라코타·연금 회색·투자 세이지, B-1 ✅ — 홈 자산 구성도 같이) · 배분 추이 모바일 200(B-5 ✅) · 툴팁 글자 본문색 + 점(F-209 ✅) · 자산 행 "갱신일"·통장 "N종목" 없음(의도적, API 필드 없음) | ✅ |
| 공통 상태 | States.png | 렌더 확인(390 iframe) | 로딩 = `SectionSkeleton`(페이지 hero+행 3 · 추이 차트 자리) · 에러 = `SectionBoundary` — 스냅샷 API 를 500 으로 막으니 "자산 추이 · 불러오지 못했어요 다시 시도", hero·보유 종목은 그대로 → 다시 시도로 복구 확인 · 빈 = "~없어요" 톤 통일 | ✅ |

## blocking — 시안·계획 불일치
| # | 발견 | 출처 | 수정안 |
|---|---|---|---|
| B-1 | 자산군 색이 **현재 비중 순위**(`chartColor(i)`)라, DESIGN §2-4 고정 매핑(chart-1 투자 · 2 현금 · 3 부동산 · 4 연금 · 5 기타)과 handoff 와 다름. 순위가 바뀌면 과거 추이 색 의미도 바뀜. 5위는 트랙색(hair)이라 빈 막대처럼 보임 | 나 · codex | `assetClassColor(c)` 고정 매핑 → wealth 구성 막대·행·배분 추이 + **홈 자산 구성(배치1)** 같이. 종목(StockShares)은 자산군이 아니라 순위 유지 |
| B-2 | 종목 0 인 투자 계좌에서 종목 비중이 "현금 100%" 로 나옴(plan: 숨김) | 나 · subagent | account-portfolio-section: 활성 종목 0 이면 StockShares 렌더 안 함 |
| B-3 | 기록 시트(모든 FormSheet)가 탭바(z500) 아래(z200)라 스크림 위로 탭바가 뜨고, 시트는 탭바 높이만큼 본문을 띄워 보정 중 | 나 | Drawer `zIndex` 를 탭바 위(600)로 올리고 `--bottom-tab-h` 보정(maxHeight·body padding·FormActions sticky bottom) 제거. FAB(150)은 그대로 시트 아래 |
| B-4 | plan 상태 3종 미구현: 로딩 스켈레톤 · 섹션 에러 재시도 · 통장 0개 거래 폼 안내 | 나 · codex · subagent | 공용 `SectionSkeleton`(hero 28 + 행 3) · `SectionBoundary`(QueryErrorResetBoundary + EmptyText "불러오지 못했어요 · 다시 시도") **신규 컴포넌트 2개(승인 필요)** + 폼 통장 0개 EmptyText(+ 통장 추가) |
| B-5 | 배분 추이 모바일 140px(plan·handoff 200) | codex · 나 | allocation-trend-chart 만 높이 200 고정 |
| B-6 | 종목 상세 섹션 제목 "거래내역" ≠ Figma 45:130 "매매 내역" | 나 | ko/en 키 `portfolio.trade_history` 추가 |

## 수정 — 품질(시안은 맞음)
| # | 발견 | 심각도 | 출처 | 수정안 |
|---|---|---|---|---|
| F-201 | FormSheet 다이얼로그에 접근 가능한 이름 없음(제목이 일반 Text) | high(a11y) | codex | 제목 Text 에 id + Drawer `aria-labelledby` |
| F-202 | 기간 칩 가로 22~31px(DESIGN 칩 히트 ≥32) — "전체" 22 | medium | 나 | FilterChip `paddingInline 6 · marginInline -6`(글자 위치 유지). 거래 필터 칩도 같이 |
| F-203 | 추이선 마지막 점·라벨이 변화 0 에도 up(빨강) | medium | 나 · subagent | trend-line `last-prev >= 0` → `signColor(last-prev, "asset")` |
| F-204 | 부호·% 표기가 한 화면에 4종(`▲ 765,900 +10.5%` · `+292,920 (▲ 17.01%)` · `-1.29%` · `-0.7%`) + 390 에서 보유 종목 메타 "평균 28…" 잘림 | medium | 나 · subagent | 보유 종목 sub = `fmtArrowPct(rate, 1)` 만(plan ⑦ "손익 ▲▼ %") · % 소수 1자리로 통일 |
| F-205 | -0.7% 변화가 급락처럼 보임 — `trendYDomain` 이 변동폭 기준 패딩이라 작은 변화도 차트 높이 40% | medium(데이터 정직) | 나 | 패딩 하한 `max * 0.03` (TrendLine 쓰는 투자 메인·계좌·종목 추이 같이 바뀜, 홈 스파크라인은 별도라 무관) |
| F-206 | 비활성 매도 버튼이 파란 글자 그대로(인라인 `c` 가 disabled 색 덮음) | medium | subagent | `c={canSell ? semanticColor("down") : undefined}` |
| F-207 | 매도 행 실현손익이 meta 와 sub 에 두 번 | medium | subagent | Figma 45:130 대로 메타("· 실현 −X")만 두고 우측 sub 제거 |
| F-208 | 아이콘 버튼 44 미만: 뒤로 36 · 새로고침 34 | medium | codex · subagent | ActionIcon 44(시각 크기 유지하려면 `::before` 히트 확장, FAB 패턴) |
| F-209 | 배분 툴팁 금액 글자에 차트색 → chart-4/5 대비 2.64/1.43 | medium | codex | 글자 = text, 앞에 6px 색 점 |
| F-210 | 삭제 버튼 `color="red"`(팔레트 밖) · 전체폭 light(plan: 푸터 좌측 subtle danger) | medium | codex · subagent | `variant="subtle" color="danger"` — 위치 변경은 8개 폼 공통이라 색만 이번에, 위치는 배치3 |
| F-211 | 문구 톤 "보유 종목이 없습니다" 등 ↔ 다른 빈 상태 "~없어요" | low | 나 · subagent | 배치2 화면 키만: `portfolio.empty_holdings`(계좌 상세) · `no_sell_history`(누적 매매수익 시트) → "~없어요" |
| F-212 | 투자 메인 오늘 날짜 UTC(`new Date().toISOString()`) — 자정~09시 KST 날짜 어긋남 | low(버그) | subagent | `todayIsoKst()` |
| F-213 | `semanticColorFor`(portfolio-section) = signColor 복제 | low | subagent | signColor 로 교체 |
| F-214 | 기간 칩이 개수 기준(1개월 = 최근 2점) — 박제 누락 달 있으면 기간이 늘어남 | low | codex | 날짜로 자르기(`date >= 기준월`) |
| F-215 | 금액 입력 초기값 "0" 이 실제 값(지우고 입력) | low | 나 | 상태는 숫자 0 유지, 입력칸만 `value={amount || ""}` + placeholder "0"(스키마 무변경) — 새 평가액 칸도 같이 |
| F-216 | 모노 라벨 `{11/16/.06em}` 인라인 11곳 · accent 헤더 링크 로컬 복붙 3곳 | low(일관성) | subagent | `.moeum-mono.moeum-label` 클래스 + `AccentLink` 공용 — 배치1 D-4 에서 미룬 "유틸 클래스" 흡수 |
| F-217 | 기록 시트가 내용과 무관하게 늘 90dvh — Mantine `--drawer-height` 가 최대 높이로 풀려 푸터 아래 빈칸(재검증 중 발견) | medium | 나 | FormSheet content `height: auto` → 내용 높이(Figma 46:451 처럼 푸터가 바닥 16 위) |

## 보류 · 결정 필요
| # | 내용 | 제안 |
|---|---|---|
| H-1 | accent 링크 대비 4.35:1 · up 11~14px 4.48:1(AA 4.5 미달, 배치1 부터) + 추가 발견 expense 3.66 · income 4.22 | ✅ **결정(사용자): 한 단계 진하게** — 라이트 accent sage.7 · up danger.7 · expense terracotta.7 · income positive.6(down info.6 유지) + `primaryShade` 라이트 7. 실측 6.21 · 6.01 · 4.99 · 5.71 · 4.80, filled 버튼 흰 글자 6.69. DESIGN §2-2·§2-4 갱신 · Figma 변수 라이트 값 동기화 |
| H-2 | 금액 입력 정렬: plan "우측" vs Figma 46:451 좌측 | Figma 정본 → 현행 유지, plan/2.md 문구 정정 |
| H-3 | 추이선 월 축: DESIGN §2-4 "축 없음" vs Figma 25:198·45:130 월 눈금 | Figma 정본 → DESIGN 문구 정정("월 눈금 모노 11 · 축선 hair, 그리드 없음") |
| H-4 | 매매 시트 submit filled danger/info · trade-form 로컬 `krw`·`gray-6` · 거래 라우트 fallback Card | plan "매매 폼 내부 = 배치3" → 배치3 폼 일괄 |
| H-5 | 데스크톱 레일 두 벌 렌더(hiddenFrom/visibleFrom) — subagent 가 `useMediaQuery` 제안 | **기각** — 프로젝트 룰(SSR 불일치로 useMediaQuery 레이아웃 분기 금지). 쿼리 키 같아 요청 1회 확인 |
| H-6 | 테마 shadows xs~lg rgba(palette 밖) | 기존 baseline 88 에 포함. 토큰 정리 때 |
| H-7 | 수동자산 섹션 제목 "부동산·연금·금" 인데 적금도 들어감 | ✅ **결정(사용자): "직접 입력 자산"** / en "Manual assets" — 거래로 움직이는 통장과 성격 차이를 제목에. 시트 제목도 같이("직접 입력 자산 추가/수정") |
| H-8 | plan 에 있지만 API 에 없는 필드: 자산 갱신일 · 통장 N종목 · 종목별 실현손익 누적 | 의도적 차이 — plan/2.md 정정. 필요하면 백엔드 후보(배치1 D-2 월간 투자손익과 함께) |

## 룰 구멍 메모
- S4 에서 Figma 가 plan 과 다르게 정한 것(금액 정렬·월 축)을 plan/DESIGN 에 되돌려 적는 단계가 없어 S6 에서 "시안 불일치" 로 다시 튀어나옴 → S4 체크리스트에 "plan·DESIGN 과 다른 Figma 결정 역반영" 추가 제안.
- S5 화면 규칙 "빈·로딩·에러 상태 확인" 이 렌더 확인만 요구해서, 로딩·에러를 렌더로 못 만들면 그냥 통과됨(B-4) → "상태 컴포넌트 존재(코드) 확인" 으로 바꾸기 제안.
- S5 가 DESIGN §2-4 색 매핑 대신 배치1 홈의 순위 규칙을 따라감(B-1) — 배치1 S6 도 못 잡음. 대조표 "차이" 열에 **색 매핑** 항목 고정 제안.
- design-review 기본 터치 기준 44 vs DESIGN 텍스트 링크·칩 32 예외 — 감사 스크립트 기준을 32 로 두고 아이콘 버튼만 44 로 따로 봐야 오탐이 없음.
- 백그라운드 탭에서 zoom 캡처가 탭바(fixed)를 가끔 빼먹음 → 겹침 판정은 `elementFromPoint` 로.
- 백그라운드 탭 iframe 에선 React Query 재시도가 `paused`(document hidden) · 연쇄 타이머 1분 단위 → 에러 상태 검증은 iframe `visibilityState` 를 visible 로 덮고 `visibilitychange` 발사.
- dev 서버가 몇 시간 뒤 파일 감시를 놓침(touch 도 무반응) → 픽스 재검증 전 "변경한 토큰이 computed 에 반영됐는지" 1회 확인 후 아니면 재시작(배치1 SSR 500 메모와 같은 계열).

## 픽스 검증 (2026-09-12, 38814e6 · 82ac282)
typecheck 0 · lint 0 · 토큰 신규 0(baseline 88) · 콘솔 에러 0(9경로 390, dev 서버 재시작 후 재확인 — 파일 감시가 멈춰 H-1 변경이 안 잡혔던 것) · H-1 실측 accent 6.21 · up 6.01 · income 5.71 · expense 4.99 · down 4.80 · 버튼 흰 글자 6.69 · shots 7장 H-1 반영 후 재촬영(wealth · wealth-desktop · invest-account · invest-account-desktop · invest-portfolio · invest-portfolio-desktop · transactions-new)
신규 공용: `SectionSkeleton` · `SectionBoundary` · `AccentLink`(section 12/600 · header 13/700) · `.moeum-mono.moeum-label` · `ASSET_CLASS_COLOR`(portfolio/constants.ts — 옛 hex 매핑 자리를 재사용, 삭제 후보에서 빠짐)
공통 영향: 탭바 z 500→190 · 시트 탭바 보정 제거(FormSheet·FormActions·가계부 전환 Drawer) · % 기본 소수 1자리(fmtSignedPct·fmtArrowPct — 홈 "+6.1%" 등 배치1 화면도) · FilterChip 가로 히트(거래 필터 칩도) · 홈 자산 구성 막대 고정 색
남은 것: 보류 H-2·H-3·H-8(plan·DESIGN 문구 정정) · H-4(배치3 폼) · H-6(토큰 정리) — H-1·H-7 은 결정 반영, H-5 기각 · 평가조정 생성 힌트가 입력 전에도 "감소 X" 로 뜨는 기존 동작(이번 범위 밖, 배치3 폼) · `value-trend-chart.tsx` 삭제(사용자 확인)
