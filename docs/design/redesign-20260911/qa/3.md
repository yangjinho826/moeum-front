# QA redesign-20260911 배치3 — 2026-09-12 @dbbd918(감사) · 픽스 f71220c
토큰 신규 위반: 0(baseline 88 유지) · design-review(감사만 — 배치1·2 와 같은 방식, 자동 커밋은 `/commit` 룰과 충돌): **blocking 4 / 수정 11 / 보류 9** → 사용자 D1 "전부", blocking 4 + 수정 11 처리 · typecheck 0 · lint 0 · 콘솔 에러 0(배치3·폼 10경로 390 iframe, 예외 H-307) · 가로 스크롤 0
외부 시각: Codex(소스 감사 7건) + Claude 서브에이전트(일관성 7 + polish 9) — 겹친 것: 소수·빈 값 입력 처리 · 색 선택 포커스 링 · 44 미만 타깃(색 28 · 아이콘 36 · 세그) · 컨트롤 접근성 이름 · `red/blue/green` 알림 색 · 폼 간격 14 vs 8
디자인 점수: 감사 전 B(시안 배치는 전부 일치, 감점 = 입력 회귀 B-1 · 다크 삭제색 B-3 · 비활성 삭제 블록 B-2) → 픽스 후 A-(남은 것 = 보류 9, 세그 높이 H-301 이 가장 큼) · AI slop A(카드 0 · 그라데이션 0 · 3열 아이콘 그리드 0) · Litmus 7개 중 YES 6(6번 모션 = 세그 인디케이터뿐, 명세서 룩 의도)

## 대조표
| ID | handoff | shot | 차이 | 판정 |
|---|---|---|---|---|
| account/[accountId] | handoff/3/shots/Main.png · AccountDesktop.png (정본 Figma 58:383) | shots/account-detail.jpg · account-detail-desktop.jpg · 다크 390 렌더 | 배치 일치(hero 28 · StatGrid · 잔액 추이 · 거래 내역 · 데스크톱 "이번 달" 레일) · 대비 라이트 4.55~11.18 / 다크 6.23~14.44 · 변화 0 이면 "0 0.0%"(F-306) · 탭바 활성 없음(F-304) | ✅(픽스 후) |
| account/new | AccountSheet.png · AccountPageDesktop.png (정본 60:77) | shots/account-new-desktop.jpg | 페이지 모드 Card 0 · 폼 560 실측 · 세그 생활·적립·투자 · 라벨 문구가 Figma(이름·유형·색)와 i18n(통장명·타입·색상) 다름(F-311) · 색 선택 포커스 링 없음(F-301) | ✅(픽스 후) |
| account/[accountId]/edit | AccountSheetEdit.png (정본 60:249) | shots/account-edit.jpg · 다크 페이지 모드 렌더 | 푸터 [삭제 x8 · 글자 21 = 필드 선 20][취소][수정] 실측 · 선택 아이콘 accent 2px 확인 · **다크 삭제 글자 #FEF2F2**(B-3) · **비활성 삭제 회색 블록이 필드 선 밖으로**(B-2, 종목 수정 시트 실측) | ✅(픽스 후) |
| 매매 시트 | TradeSheet.png · TradeSheetEdit.png (정본 61:115) | shots/trade-sheet.jpg · 데스크톱 모달 560 · r12 실측 | 2열 · 정산 요약 · 기록 버튼 accent(#4F6149) · 50×12,418 → 620,900 · **수량 "1.5" → "5"**(B-1) · 수수료 0 에 "+0원"(F-307) · 날짜 placeholder 형식 다름(F-308) | ✅(픽스 후) |
| /account | ⏭ | 리다이렉트 실측(/ko/account → /ko/wealth) | — | ✅ |
| 상태·힌트 | States.png | 390 iframe 실측 | 평가조정: 입력 전 "잔액 620,000,000원" 만 → 630,000,000 입력 후 "· 10,000,000원 평가증가"(up) ✅ · **빈 새 평가액 제출 = 잔액 전액 감소**(B-4) · 삭제 불가 힌트 표시 ✅ | ✅(픽스 후) |

## blocking — 회귀·시안 불일치
| # | 발견 | 출처 | 수정안 |
|---|---|---|---|
| B-1 | 수량에 "1.5" 를 치면 "5" 가 남음(소수 주식 불가) · 시작 잔액 "-" 로 음수 입력 불가 — S5 에서 넣은 `onChange` 가 중간 문자열("1." · "-")을 0 으로 바꿈 | codex · 나(실측) | trade-form `numProps` · account form 시작 잔액: 빈 값만 0, 나머지는 `getInputProps(f).onChange(v)` 그대로(제출 직전 숫자화는 이미 있음). 화면 빈칸(`|| ""`)은 유지 |
| B-2 | 비활성 삭제(저장 중 매번 · 종목 보유 시 상시)가 Mantine `:disabled` 배경으로 **회색 블록**이 되고 `ml -12` 때문에 필드 선 밖으로 삐져나옴 | subagent · 나(실측) | 삭제 버튼에 `moeum-remove-action` 클래스 → globals `:disabled` 배경 투명(글자만 disabled 색) + `removeHint` 를 `aria-describedby` 로 연결 |
| B-3 | 다크에서 삭제 글자가 **#FEF2F2(거의 흰색)** — `--mantine-color-danger-light-color` 가 다크 primaryShade 4 기준 `danger.0` 로 풀림(Mantine 계산). 파괴적 행동인데 중립 글자로 보임 | 나(실측) · codex(셰이드 없는 danger) | 삭제 버튼 `--button-color: var(--mantine-color-danger-text)`(라이트 #B91C1C · 다크 #F87171) — 새 토큰 없이 테마 변수. disabled 는 Mantine 규칙이 그대로 회색 처리 |
| B-4 | 새 평가액을 비운 채(0) "추가" → `diff = −잔액` 으로 **자산 전액 감소 거래 생성**. 원래 있던 위험인데 S5 힌트 변경으로 경고성 문구("감소 <잔액>")마저 사라짐 | subagent · 나(코드 확인) | 거래 폼 평가조정 생성: `valuation <= 0` 이면 필드 에러 "새 평가액을 입력해 주세요"(ko/en 키) 후 중단. 0 원 처리는 자산 삭제로(수동자산 시트) |

## 수정 — 품질(시안은 맞음)
| # | 발견 | 심각도 | 출처 | 수정안 |
|---|---|---|---|---|
| F-301 | 색 선택 버튼 인라인 `outline: none` 이 키보드 포커스 링을 지움 · 선택 링과 포커스 링 구분 없음 | high(a11y) | codex · subagent | 선택 = `box-shadow: 0 0 0 2px surface, 0 0 0 4px text`, outline 은 Mantine focus-visible 에 맡김 |
| F-302 | 터치 타깃 44 미만: 색 원 28 · 아이콘 칸 높이 36 | medium | codex · subagent · 나(실측) | 색 = 44 버튼 안 28 원(시각 그대로, 간격 0) · 아이콘 칸 높이 44 |
| F-303 | 유형 세그·색·아이콘 묶음에 접근 가능한 이름 없음(`Input.Wrapper` label 의 for 대상 없음) · 색은 hex, 아이콘은 영문 키로 읽힘 | medium(a11y) | codex · subagent | 묶음 `role="group"`(세그는 radiogroup) + `aria-labelledby` · 색 `"{라벨} N"`(i18n) · 아이콘 이름 i18n 16개 |
| F-304 | `/wealth` · `/account/*` 에서 탭바 활성 탭 없음(홈 매칭이 루트만) — "지금 어디" 가 안 보임. Figma 46:195 는 투자 활성, 58:383 은 홈 활성으로 서로 다름 | medium | 나 | bottom-tab·sidebar 홈 매칭에 `/wealth`·`/account` 추가(자산 = 홈의 하위) · Figma 46:195 탭바 홈으로 |
| F-306 | 지난달과 잔액이 같으면 "0 0.0% · 지난달보다" | low | subagent · 나 | 통장 상세만: delta 0 이면 "지난달과 같아요"(dim) |
| F-307 | 수수료 0 에 "+0원" | low | subagent | 0 이면 부호 없이 |
| F-308 | 날짜 placeholder "YYYY-MM-DD" ↔ 표시 "YYYY.MM.DD"(매매·거래 폼) | low | subagent | placeholder "YYYY.MM.DD" |
| F-309 | 수수료 힌트만 "~됩니다"(나머지 "~어요") | low | subagent | "매수 수수료는 평단가에 포함돼요" · "매도 수수료는 실현손익에서 빠져요" |
| F-310 | 페이지 모드 통장 **수정** 저장 후 /wealth 로 감(취소는 back) | low | subagent | 수정 → `/account/[id]`, 생성·삭제 → /wealth |
| F-311 | Figma 60:77·60:249 필드 라벨(이름·유형·색)이 코드 i18n(통장명·타입·색상)과 다름 | low | 나 | 문구 정본 = i18n → Figma 텍스트를 코드 문구로 |
| F-312 | 삭제 불가 힌트가 스크린리더에 버튼과 연결 안 됨 | low(a11y) | subagent | B-2 와 같이 `aria-describedby` |

## 보류 · 결정 필요
| # | 내용 | 제안 |
|---|---|---|
| H-301 | SegmentedControl 높이 ~34(44 미만) — 테마 기본 `sm`, 배치1 부터 모든 세그 | 테마 `SegmentedControl` 기본 `size: "md"`(전 화면 영향 → 결정 필요) |
| H-302 | 폼 간격 14(통장·매매·거래 = Figma 14) vs "sm" 8(카테고리·고정비·가계부·종목·자산) | 배치4·5 폼 화면 때 14 로 통일 + DESIGN §5 에 "폼 간격 14" 명시 |
| H-303 | 시작 잔액·단가·수수료도 금액 28/56 로? | 기각 — Figma 60:77·61:115 는 44 필드. DESIGN "금액 입력만 28/56" 을 "거래 금액 한 칸" 으로 문구 정정 |
| H-304 | 인풋 단위 라벨("원"·"주") 복붙 5곳(12px `pr 8` 변형 2) | 다음 폼 배치에서 `InputUnit` 공용 추출 |
| H-305 | 카테고리 종류 Select 2개(§5 = 세그) · 종목 조회 버튼 `variant="light"` | 배치4 · 배치7 |
| H-306 | 이번 달 수입·지출이 0 이어도 의미색 | 0 = 방향 없음 → dim? 거래 요약(배치1)과 같은 규칙이라 전역 결정 |
| H-307 | `/household/new` textarea `value=null` 경고(콘솔) | 배치5 가계부 폼(기존 버그) |
| H-308 | StatGrid "지출" = 일반 지출(고정 제외, `monthlyFlows.expense`) — 합산 아님은 코드로 확인. 라벨이 총지출로 읽힐 수 있음 | "일반 지출" 로? 문구 결정 |
| H-309 | 알림·확인 색 `red`/`blue`/`green`(팔레트 이름) — 매매·거래·카테고리·고정비·통장 훅 전부(기존) | 별도 정리 1커밋: 성공 `positive` · 실패/삭제 `danger` · 매수 알림 틴트 제거 |

## 픽스 검증 (f71220c, 390 iframe · 1440 · dev 서버 재시작 후)
| # | 실측 |
|---|---|
| B-1 | 매매 수량 "1.5" 유지 · 1.5 × 10,000 → 정산 15,000원 / 통장 시작 잔액 "-5,000" 입력 |
| B-2 | 종목 수정 시트(보유 60주) 비활성 삭제 배경 `rgba(0,0,0,0)` · 글자 disabled 회색 · 힌트 `aria-describedby` 연결 |
| B-3 | 다크 통장 수정 삭제 글자 `rgb(248,113,113)` = danger.4(라이트는 #B91C1C) |
| B-4 | 수동자산(우리집 아파트) + 새 평가액 빈 칸 + 추가 → "새 평가액을 입력하세요" · POST 0건(QA 가 요청 가로채 확인) |
| F-301 | Tab 으로 "색 2" 포커스 → `:focus-visible` outline 2px accent, 선택 링(본문색)과 따로 보임 |
| F-302 | 색 44×44(원 28) · 아이콘 39×44(8열 모바일 폭) |
| F-303 | radiogroup "타입" · group "색상" · group "아이콘" · 매매 radiogroup "매매 구분" · 색 "색 N" · 아이콘 한글 이름 |
| F-304 | 활성 탭 = /ko 홈 · /ko/wealth 홈 · /ko/account/* 홈 · /ko/transactions 거래 · /ko/invest/* 투자. FAB = /ko·/ko/transactions(·/new)만, /wealth·/account 없음(재검증 중 발견한 FAB 확산을 `isHomeRoot` 로 분리) |
| F-306 | 통장 상세 "지난달과 같아요"(0 변화) |
| F-307·308·309 | 수수료 0 → "0원" · 날짜 placeholder YYYY.MM.DD · "평단가에 포함돼요" |
| F-310 | 코드 확인(수정 → /account/[id], 생성·삭제 → /wealth) |
| F-311 | Figma 60:77·60:249 라벨 통장명·타입·색상 · 61:115 거래일·(선택) · 아이콘 칸 44 · 46:195 탭바 홈 활성 |

typecheck 0 · lint 0 · 토큰 신규 0(baseline 88) · 콘솔 에러 0(12경로 390, 예외 H-307) · 가로 스크롤 0 · shots 5장 재촬영(account-detail · -desktop · account-edit · account-new-desktop · trade-sheet)
중간 사고: dev 서버가 편집 누적으로 `/wealth` SSR 500(`useContext null`, 학습 기록의 그 증상) → 재시작 후 정상. 코드 원인 아님.

남은 것: 보류 H-301~H-309(결정 필요 H-301 · H-306 · H-308 · H-309, 나머지는 다음 배치·문구 정정)

## 룰 구멍 메모
- build-qa S5 "화면별 렌더" 가 **입력 동작**(소수·음수·빈 값)을 안 봄 → 폼 배치는 필드 타이핑 실측을 게이트에 넣어야 B-1 같은 회귀를 S5 에서 잡음.
- 다크 스킴 샘플이 S6 에서만 나옴 → 공통 컴포넌트(버튼 variant) 변경 시 S5 에서 다크 1장.
