# QA redesign-20260911 배치4 — 2026-09-13 @2ab92a8(감사) · 픽스 d3b63c5 · DESIGN e30180b
토큰 신규 위반: 0(baseline 88 유지) · design-review(감사만 — 배치1~3 과 같은 방식, 자동 커밋은 `/commit` 룰과 충돌): **blocking 0 / 수정 9 / 보류 6** · 사용자 "S6 까지 쭉"(단계별 승인 생략) → 수정 9 전부 처리 · typecheck 0 · lint 0 · 콘솔 에러 0(8경로) · 가로 스크롤 0
외부 시각: Codex(소스 감사 10) + Claude 서브에이전트(일관성 10) — 겹친 것: 요약 실패가 목록까지 가림 · 월 연타 누락 · 불러온 페이지 기준 개수 · 폼 페이지 격자 vs DESIGN §5 · 간격 14·`pt 48` 일회성 값
디자인 점수: 감사 전 B+(시안 배치 일치, 감점 = 요약 경계가 수정 진입까지 막음 · 월 연타 · "예정 NaN"(S5 에서 잡음)) → 픽스 후 A-(남은 것 = 보류 6) · AI slop A(카드 0 · 아이콘 박스 0 · 배지 0) · Litmus 7개 중 YES 6(6번 모션 = 지연 월 흐림 0.5 뿐, 명세서 룩 의도)

## 대조표
| ID | handoff | shot | 차이 | 판정 |
|---|---|---|---|---|
| category | Main.png · CategoryDesktop.png (정본 Figma 67:1013) | shots/category.jpg · category-desktop.jpg · 다크 1440 렌더 | 지출 12 · 수입 7 섹션 · 18px 글리프(카테고리 색) · chevron · 레일 도움말 · 내정보 탭 활성(탭바·사이드바) · **시드 아이콘 이름이 선택기 밖이면 요술봉**(S5 에서 `isKnownIcon` → 6px 점) · 개수는 실데이터(시안 14 · 5 는 예시) | ✅ |
| category/new | CategorySheet.png · CategoryPageDesktop.png (정본 67:1061) | shots/category-new-desktop.jpg | 좌 폼 720 · 우 도움말(분류 · 정렬 · 삭제) · 분류 세그 44 · 정렬 빈칸 + placeholder 0 + 설명 · 입력 실측(키 입력): "12" → 12 · 지우면 빈칸 · "-1.5" → "15"(음수·소수 차단) | ✅ |
| category/[categoryId] | CategorySheetEdit.png (정본 67:1172) | shots/category-sheet.jpg | 수정 시트 = 분류 수입 선택 · 아이콘 지갑 accent 링 · 정렬 1 · [삭제][취소][수정] · **시드 색이 팔레트 밖이라 색 선택 없음**(H-403) · 제출 X | ✅ |
| fixed | Fixed.png · FixedDesktop.png (정본 67:1285) | shots/fixed.jpg · fixed-desktop.jpg · 다크 1440 렌더 | MonthPicker · hero 사용 합계 · 캡션 기록/미기록 · 행 글리프 · 매월 N일 · 사용액(0 dim) · chevron · 레일 3행 · **시안의 "예정" 금액 제거 — 백엔드 FixedResponse 에 금액 없음**(S5 "예정 NaN" 렌더로 발견, Figma 행 Sub 숨김 · 캡션 동기화) · uitest 데이터는 연결 거래 0 이라 전 행 0 | ✅(픽스 후) |
| 폼 페이지 4(거래 · 고정지출 · 가계부 · 종목) | ⏭(해당 배치) | 1440 렌더 | 좌 폼 칼럼 · 우 레일 비움(문구는 해당 배치) · 깨짐 0 · "고정 지출" → "고정지출"(거래 유형 세그 포함) | ✅ |

## blocking — 회귀·시안 불일치
없음. (S5 중 발견·수정: 고정지출 "예정 NaN" — 계획표가 없는 필드를 전제, 백엔드 `app/domain/fixed/schema.py` FixedResponse 에 amount 없음 확인 후 표시 제거)

## 수정 — 품질(시안은 맞음)
| # | 발견 | 심각도 | 출처 | 수정 |
|---|---|---|---|---|
| F-401 | 월 요약(보조 조회)이 실패·지연되면 `SectionBoundary` 가 hero 와 **행 목록(수정 진입)까지** 스켈레톤/에러로 바꿈 — 컴포넌트 주석("주 쿼리는 감싸지 않는다")과도 어긋남 | high | codex · subagent | hero·레일만 경계, 행은 같은 캐시를 비-Suspense `useQuery(keepPreviousData)` 로 읽어 금액만 비움. 레일 경계에 월 제목 |
| F-402 | 경계가 한 번 실패하면 다른 달로 옮겨도 에러 유지(`failed` 상태) | medium | codex | `SectionBoundary resetKey` — 바뀌면 `QueryErrorResetBoundary.reset` + 실패 해제 |
| F-403 | 월 이동이 transition 안이라 선택기 라벨이 데이터 올 때까지 안 바뀌고, 연타가 이전 값 기준으로 겹침 · 진행 표시 없음 | medium | codex · subagent | 월은 즉시, 요약은 `useDeferredValue` + 옛 값 동안 opacity 0.5(realized-pnl-panel 과 같은 방식) |
| F-404 | 개수·분류 섹션·기록/미기록이 **불러온 페이지(30)** 기준 — 지출이 30 넘으면 수입 섹션이 스크롤 전까지 없음 | medium | codex · subagent | 관리 목록 한 페이지 100(백엔드 상한 200), 넘치면 센티널 |
| F-405 | 행 앞 글리프 stroke 1.5 — DESIGN §4 리스트 아이콘 stroke 2 와 다름 · 장식인데 스크린리더 노출 | medium | subagent · codex | stroke 2 · 래퍼 `aria-hidden`. Figma Icon/* 16 선 굵기 2 로(28 벡터) |
| F-406 | 도움말 레일이 generic div — 보조 콘텐츠로 건너뛸 수 없음 | medium(a11y) | codex | `FormGuide` = `aside aria-label="도움말"` · 카테고리 목록 레일도 FormGuide 재사용(인라인 스타일 복제 제거) |
| F-407 | 둘 다 수정 시트를 여는데 카테고리 행만 chevron | low | subagent | 고정지출 행 chevron(Figma 69:297~394 동기화) |
| F-408 | 미기록 개수가 0 초과여도 dim — 챙겨야 할 숫자가 흐림 | low | subagent | `amountColor(n, "text")` — 0 만 dim(기록도 같게) |
| F-409 | 뒤로 = `router.back()` — 새로고침·직접 진입이면 앱 밖으로 | low | subagent | 멤버 화면처럼 `back=/{locale}/settings` · en `month_total` "Fixed in {month}" |

## 보류 · 결정 필요
| # | 내용 | 제안 |
|---|---|---|
| H-401 | 고정지출 정의가 0개면 빈 상태만 — 지운 항목의 이번 달 사용액(usages 에 남음)은 안 보임 | 드문 경우라 유지. 필요하면 빈 상태 위에 hero 만 |
| H-402 | 세그먼트 전체 44 지만 라벨(클릭 영역)은 36 — 트랙 패딩 4 는 누를 수 없음(배치3 H-301 테마 방식) | 테마 `label` 최소 높이 44 − 트랙 패딩으로 보정할지 전역 결정 |
| H-403 | 시드 카테고리 색·아이콘이 선택기(색 12 · 아이콘 16) 밖 — 수정 시트에서 선택 표시 없음, 목록은 점으로 대체 | 시드 데이터를 선택기 값으로 옮기는 백엔드 마이그레이션(묻고 진행) 또는 선택기에 "현재 값" 칸 |
| H-404 | 고정지출 폼 "금액" 필드 — 백엔드에 없어 저장 안 되는 유령 필드(수정 시트에서 빈칸) | 배치5 고정지출 폼에서 제거(또는 백엔드에 금액 추가 = 기능 결정) |
| H-405 | 행 글리프 색 = 사용자 색 — 노랑(#FCD34D 등) 크림 배경 대비 ~1.3:1, 빨강·파랑이 상승/하락 색과 겹침 | 장식(aria-hidden, 제목이 이름을 말함)이라 AA 비대상 · pick A 결정이라 유지. 거슬리면 글리프 text-dim + 색 점 |
| H-406 | 일회성 값: 레일 `pt={48}` 11파일 · 섹션 제목 개수 span 2곳 · 가계부·종목 폼 간격 `sm` | `.moeum-rail` 클래스 · `Section count` prop · 폼 간격은 배치5·7 에서 14 |

## 픽스 검증 (d3b63c5, 1440 · 390 iframe)
| # | 실측 |
|---|---|
| F-401 | `monthly-summary` 를 500 으로 가로채고 8월로 이동 → hero·레일만 "불러오지 못했어요 · 다시 시도", 행 14개 · chevron 그대로(금액만 빈칸) |
| F-402 | 같은 상태에서 9월로 이동 → 에러 해제, "9월 고정지출" 0원 · 레일 복구 |
| F-403 | 이전 달 3회(60ms 간격) → 2026.08 → 2026.05, hero "5월 고정지출" 따라옴 |
| F-404 | 코드(PAGE_SIZE 100) · 카테고리 19 · 고정지출 14 = 1페이지 |
| F-405 | 글리프 `stroke-width="2"` · `[aria-hidden] svg` · Figma Icon/* 28 벡터 2 |
| F-406 | `aside[aria-label=도움말]` (카테고리 목록 · 폼 페이지) |
| F-407·408 | 1440 고정지출: 행 chevron · 레일 미기록 "14개" 본문색 · 기록 "0개" dim |
| F-409 | 고정지출 ← → `/ko/settings` |

typecheck 0 · lint 0 · 토큰 신규 0(baseline 88) · 콘솔 에러 0(category · fixed · category/new · account/new · fixed/new · household/new · transactions/new · settings) · 다크 1440 category · fixed · category/new 렌더 확인 · shots 4장 재촬영(category · -desktop · fixed · -desktop)
중간 사고: `/ko/category` SSR `useContext null` 예외 — dev 서버 옛 컴파일(카테고리 파일 stash/pop 재컴파일 후 0, 배치3 과 같은 증상). 코드 원인 아님.

## 룰 구멍 메모
- S3 계획표가 **백엔드 응답 필드**를 확인하지 않고 프론트 타입(`amount`)만 믿음 → "예정 금액"을 시안·Figma·i18n 까지 끌고 감. 계획표 단계에 "표시할 필드 = 백엔드 스키마에서 확인" 한 줄이 필요.
- Chrome 확장 `type` 액션은 react-number-format 숫자 인풋에 안 들어감(값 빈칸) → 숫자 입력 실측은 `key` 액션으로. 모르면 입력 회귀로 오진.
- 보조 쿼리 `SectionBoundary` 는 "무엇을 감싸나"(수정 진입까지 감싸면 안 됨)를 S5 체크에 넣어야 F-401 을 S5 에서 잡음.
