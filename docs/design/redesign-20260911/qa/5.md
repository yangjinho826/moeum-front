# QA redesign-20260911 배치5 — 2026-09-13 @2c691ba(감사) · 픽스 4f5e693
토큰 신규 위반: 0(baseline 88 유지) · design-review(감사만 — 배치1~4 와 같은 방식) : **blocking 0 / 수정 14 / 기각 1 / 보류 8(H-502·H-508 해결)** · 사용자 "S6 까지 쭉" → 수정 14 전부 처리 · typecheck 0 · lint 0 · 콘솔 에러 0
외부 시각: Codex(소스 감사 6) + Claude 서브에이전트(일관성 10 + HEAD 기준 4 — 작업 트리의 Codex 픽스로 이미 해결) — 겹친 것: 수정 모드 Select 다시 누르면 비워짐 · 옵션 조회 Suspense 가 폼을 가림 · 선택기 설명 미연결 · en "1th"
디자인 점수: 감사 전 B(시안 배치 일치, 감점 = 수정에서 카테고리 비우기 저장 안 됨 · 멤버 행 무반응 설명 없음(모바일) · 일회성 12px 라벨) → 픽스 후 A-(남은 것 = 보류 8, 대부분 백엔드·실데이터) · AI slop A(카드 0 · 아이콘 박스 0 · 배지 0) · Litmus 7개 중 YES 6(모션 = 시트 전환뿐, 명세서 룩 의도)

## 대조표
| ID | handoff | shot | 차이 | 판정 |
|---|---|---|---|---|
| fixed/new | FixedSheet.png · FixedPageDesktop.png (정본 Figma 73:297) | shots/fixed-sheet.jpg · fixed-new-desktop.jpg | 이름(추가 첫 포커스) · 카테고리 Select(지출만 12개 · 항목 앞 6px 색 점 · 비우기) · 결제일 NumberInput + "일" `InputUnit`("32" → "3", 1~31 strict) · 색·아이콘(설명 "비우면 카테고리 색·아이콘", 원 아래 6 띄움) · 금액 없음(유령 필드 제거) · 레일 결제일 · 사용액 · 삭제 · 제출 X | ✅ |
| fixed/[fixedId] | FixedSheetEdit.png (정본 73:406) | shots/fixed-sheet-edit.jpg | + 보관 Switch(라벨·설명 좌 · 스위치 우 · 행 44) · 비우기 설명 없음(수정은 비우기 불가) · [삭제][취소][수정] · 옛 shot 은 가로 오프셋 크롭이라 재촬영 · 제출 X | ✅(픽스 후) |
| fixed(목록 보관 섹션) | Fixed.png (67:1285 "보관 1") | shots/fixed-archived-desktop.jpg | 사용자 허락(H-502)으로 로컬 DB 에 테스트 항목 "QA 보관 테스트"(빨강 · 하트) 추가 → 보관 켜고 저장: 목록 맨 아래 "보관 1" · 제목·글리프 dim · 사용액 0 이라 금액 없음 · chevron · 섹션 제목 "고정지출 14" · 레일 미기록 14(보관 제외) · 수정 시트 다시 열면 보관 켜짐(저장 왕복) · 거래 기록 고정지출 선택지 14개에 없음(백엔드 is_archived 필터) → 삭제(소프트 삭제)로 원상복구 | ✅ |
| household | Household.png · HouseholdDesktop.png (정본 73:517) | shots/household.jpg · household-desktop.jpg | SubHeader(← 내정보 · 추가) · "가계부 N" · 행 = 이름 / 역할 · 시작일 · "사용 중" 본문 13/500 dim · 소유자만 chevron · 모바일 멤버 안내 한 줄(멤버 행 있을 때만 — uitest 는 전부 소유자라 렌더 조건만 확인) · 레일 역할 · 전환 · 검색·"멤버 0명"·카드 제거 | ✅(픽스 후) |
| household/new | HouseholdSheet.png · HouseholdPageDesktop.png (정본 73:541) | shots/household-sheet.jpg · household-new-desktop.jpg | 이름 · 설명 Textarea(placeholder) · 시작일 `YYYY.MM.DD` KST 오늘 + 설명 · 통화 없음 · 레일 가계부 · 시작일 · 삭제 | ✅ |

## blocking — 회귀·시안 불일치
없음.

## 수정 — 품질(시안은 맞음)
| # | 발견 | 심각도 | 출처 | 수정 |
|---|---|---|---|---|
| F-501 | 수정 모드 카테고리 — `clearable={false}` 여도 고른 항목을 다시 누르면(`allowDeselect` 기본 true) null → 저장 "성공"인데 백엔드가 null 을 무시해 다시 열면 그대로 | high | codex · subagent | `allowDeselect={!isUpdate}` |
| F-502 | 옵션(`formOptions`) `useSuspenseQuery` — 캐시 없이 실패하면 시트의 로딩 전용 Suspense 를 지나 라우트 에러로 목록·시트까지 사라짐, 페이지 모드는 폼 전체가 PageLoader | medium | codex · subagent | 비-Suspense `useQuery` — 늦거나 실패해도 이 필드만 비활성 |
| F-503 | 보관 Switch 누를 수 있는 높이 ~40(트랙 20) — DESIGN §4 44 미달 | medium | codex | 테마 Switch `body` minHeight 44 · 가운데 정렬 |
| F-504 | 색·아이콘 선택기 설명이 그룹에 연결 안 됨 — 스크린리더가 "다시 누르면 비움"을 못 읽음 | medium(a11y) | codex · subagent | `descriptionProps.id` + 그룹 `aria-describedby` |
| F-505 | 빈 카테고리 문구 "카테고리가 없어요" — 수입만 있을 때도 뜸 · 비활성 placeholder 라 대비 ~2:1 | low | codex · subagent | "지출 카테고리가 없어요 — 비워 둬도 돼요" 를 **설명 줄**로(불러오는 중 = placeholder, 실패 = 설명 "불러오지 못했어요") |
| F-506 | en `day_unit` "th" → "1th · 2th · 21th" | low | codex · subagent | `""` — 단위 없으면 `InputUnit` 자체를 안 그림 |
| F-507 | 멤버 행이 왜 안 눌리는지 모바일엔 설명 없음(레일 도움말은 `visibleFrom="lg"`) — chevron 만 빠진 같은 행 | high | subagent | 모바일(`hiddenFrom="lg"`) 목록 아래 13 dim 한 줄 = 레일 `role_body` 문구, 멤버 행이 있을 때만. Figma 73:517 `OwnerHint (mobile)` 75:174 |
| F-508 | "사용 중" 이 값 자리에서 인라인 `fontSize:12` + 본문 서체 덮어쓰기 — 12 는 §3 스케일 밖, 일회성 | medium | subagent | `ListRow valueText` — 글자 값은 본문 서체 13/500(모노는 한글 사이 공백이 벌어져 실측 후 채택) · DESIGN §5 ListRow 갱신 · Figma 값 13 |
| F-509 | 계획 ③ "항목 앞 6px 색 점" 미구현 · 로딩 중에도 "선택 안 함"+비활성이라 '없음'처럼 보임 | medium | subagent | `renderOption` 색 점(목록 lead 와 같은 색, 체크 표시는 직접 그림) · 로딩 placeholder "불러오는 중..." |
| F-510 | 숨긴 통화에 `z.string().min(3).max(3)` 이 남아 상세 currency 가 3글자가 아니면 보이지 않는 에러로 저장이 조용히 막힘 | medium | subagent | 검증 삭제(값은 상세 그대로 돌려보냄) |
| F-511 | 선택기 설명이 그리드 음수 마진 탓에 원 바로 아래 ~2px 에 붙음 | low | subagent | 설명 `mt 6`(색·아이콘 같게) |
| F-512 | Switch 행 레이아웃(`space-between` · `labelWrapper flex 1` · `labelPosition left`)이 호출부에만 — 다음 Switch 는 모양이 달라짐 | low | subagent | 테마 `Switch.extend` defaultProps + styles 로 이동 · DESIGN §5 |
| F-513 | 보관 행 제목만 dim 이고 lead 글리프는 원래 색 — 활성처럼 읽힘 | low | subagent | 보관 행 lead 색 null(= dim). Figma 69:394 글리프 dim |
| F-514 | en 새 문구 "ledger" vs 기존 "Household" 섞임 | low | subagent | 새 문구 5개 household 로 통일 |

## 기각
| 발견 | 이유 |
|---|---|
| 고정지출 목록 첫 섹션이 hero 바로 아래인데 hairline 기본값(true) — Section 주석 "hero 바로 아래면 false" (subagent) | compact hero 뒤 첫 Section 은 계좌 상세(account-portfolio) · 계좌 리포트도 기본 구분선 — 코드 관례 · 배치4 승인 shot · Figma 67:1285 와 같음. 주석이 관례보다 좁게 적힌 것(룰 구멍 메모) |

## 보류 · 결정 필요
| # | 내용 | 제안 |
|---|---|---|
| H-501 | 수정에서 카테고리·색·아이콘을 비울 수 없음 — 백엔드 `update_fixed_expense` 가 `if req.x is not None` 이라 null = "안 바꿈" | ✅ 닫음 — 사용자 결정 "H-501 하지 마"(백엔드 변경 안 함). 프론트는 추가에서만 비우기, 수정은 막음(DESIGN §9 2026-09-13) |
| H-502 | 보관 섹션을 실데이터로 못 봄 | ✅ 해결 — 테스트 항목 추가 → 보관 → 확인 → 삭제(대조표 행 · shots/fixed-archived-desktop.jpg) |
| H-503 | 카테고리 옵션 실패 시 "다시 시도" 없음(React Query 자동 재시도 후 설명만) · 지출 카테고리 0 이면 카테고리 관리로 가는 링크 없음 | 선택 항목이라 유지. 필요하면 설명 줄을 AccentLink(카테고리 관리)로 |
| H-504 | 선택기 `aria-pressed`(토글 의미)인데 수정 모드에선 다시 눌러도 안 풀림 | 라디오 동작과 같아 체감 문제 없음. 정확히 하려면 `role=radio` + `aria-checked` 로 전환(선택기 공통 — 배치 밖) |
| H-505 | 가계부 전환기(household-switcher)가 sage/terracotta 를 직접 씀 | 셸 배치에서 의미색으로 |
| H-506 | 종목 폼 설명 순서(label · input · description) | 배치7 종목 폼에서 |
| H-507 | 가계부 목록 응답에 멤버 수 없음 — "멤버 N명" 표시 불가 | 필요하면 백엔드 HouseholdResponse 에 member_count |
| H-508 | 참조 0 파일·키: `household/components/search.tsx`(지운 `search_term*` 키를 참조) · `household/components/table.tsx` · `household/hooks/use-sub/use-search.ts` · `HouseholdSearchRequestType` · ko/en `fixed.search_term`·`search_term_placeholder` | ✅ 사용자 확인 후 삭제 60fcc39. `household.member_count` 는 전환기가 써서 유지 |

## 픽스 검증 (4f5e693, 1440 · 390 iframe)
| # | 실측 |
|---|---|
| F-501 | 코드 `allowDeselect={!isUpdate}` · `clearable={!isUpdate}` (수정 시트 제출 X 라 저장 왕복은 코드 근거) |
| F-502·505·509 | 1440 `/ko/fixed/new` 드롭다운: 지출 12개 · 항목마다 6px 색 점 · 고른 "교통" 에 체크 + 점 · 입력 우측 비우기 × |
| F-503·512 | 390 수정 시트: "보관" 라벨·설명 좌 · 스위치 우 끝 · 호출부 styles 없음(테마) |
| F-504 | 390 `/ko/fixed/new`: 색 그룹 `aria-describedby` → "비우면 카테고리 색·아이콘을 써요(다시 누르면 비움)" · margin-top 6px. 아이콘 그룹은 설명이 없어(공용 한 줄이 색 아래) 연결 없음 — 아이콘에도 읽히게 하려면 선택기에 외부 설명 id prop(보류 H-504 와 같이) |
| F-506 | 코드: `t("day_unit")` 가 빈 문자열이면 `rightSection` undefined(next-intl 빈 문자열 그대로 반환은 서브에이전트 확인) |
| F-507 | 조건 `items.some(role !== OWNER)` — uitest 2개 모두 소유자라 미표시(정상), Figma 390 에 멤버 행 + 안내 |
| F-508 | 1440·390 "사용 중" 13px/500 본문 서체 dim, 공백 정상 |
| F-510 | zod 스키마 name 만 |
| F-511 | 설명 margin-top 6px(computed, F-504 행) |
| F-513 | Figma 69:394 글리프 dim · 코드 lead color null |
| F-514 | en.json "ledger" 새 문구 0(기존 `transactions.ledger` "Transactions" 는 다른 뜻) |

typecheck 0 · lint 0 · 토큰 신규 0(baseline 88) · 콘솔 에러 0(fixed · fixed/new · household) · shots 5장 재촬영(fixed-sheet · fixed-sheet-edit · fixed-new-desktop · household · household-desktop)
중간 사고: `/ko/household` 500 "module factory is not available … HMR" — dev 서버 옛 컴파일(재시작 후 200, 배치3·4 와 같은 증상). 코드 원인 아님.

## 룰 구멍 메모
- Section 주석 "화면 첫 섹션이 hero 바로 아래면 false" 가 실제 관례(compact hero 뒤는 구분선 유지, 큰 hero·레일 첫 섹션만 false)보다 넓어 감사가 오탐 — 주석을 관례대로 좁히거나 DESIGN §5 Section 에 한 줄.
- 서브에이전트 감사가 작업 트리 변경(Codex 픽스 반영 중)과 겹쳐 4건이 중복 보고 — 두 외부 시각은 **같은 커밋 기준**으로 돌리고, 픽스는 둘 다 끝난 뒤 반영.
- Mantine `renderOption` 은 체크 아이콘까지 대신 그린다(기본 content 교체) — 색 점 같은 장식을 넣으면 `checked` 표시를 직접.
- 숨은 창에서 Drawer 는 rAF 가 멈춰 JS `click()` 로 연 상태가 화면에 늦게 뜸 → 그 사이 실제 클릭이 오버레이를 눌러 닫음. 시트 shot 은 **한 가지 방법으로만 열고** screenshot 으로 프레임을 돌려 기다린다.
