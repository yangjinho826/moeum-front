# QA redesign-20260911 배치6 — 2026-09-13 @49ed452(감사) · 픽스 e14f503
토큰 신규 위반: 0(baseline 88 유지) · design-review(감사만 — 배치1~5 와 같은 방식) : **blocking 0 / 수정 13 / 기각 1 / 보류 5** · 사용자 "전체 총괄" 위임 → 수정 13 전부 처리 · typecheck 0 · lint 0 · 콘솔 에러 0
외부 시각: Codex(소스 감사 4) + Claude 서브에이전트(디자인·일관성 10 + polish 5) — 둘 다 같은 커밋(49ed452) 기준(배치5 룰 구멍 반영). 겹친 것: 아이콘 선택기 칸 폭 · 이메일 검사 불일치
디자인 점수: 감사 전 B(시안 일치, 감점 = 폼 아닌 시트에 닫기 없음 · "초대"가 실제 동작과 다름 · 로딩 시 시트 크기 튐) → 픽스 후 A-(남은 것 = 보류 5, 실데이터·셸 범위) · AI slop A(카드 0 · 아바타 0 · 왕관 0 · 그라데이션 0) · Litmus 7개 중 YES 6

## 대조표
| ID | 정본 | shot | 차이 | 판정 |
|---|---|---|---|---|
| household/[householdId]/members | Figma 76:111(시트) | shots/members-sheet.jpg · members-desktop.jpg | 시트: 제목 + 닫기 X · "우리 가족 멤버 1"(모노 dim 숫자) · 행 = 이름 / 소유자 · 이메일 · "멤버 추가" 필드 + 추가 버튼 한 줄(비었으면 비활성) + 설명 · 페이지: SubHeader(← 내정보) · 레일 추가 · 권한 · 내보내기. 형식 오류·미가입 이메일 → 필드 아래 role=alert + 포커스(실측, 사용자 검색 조회만 — 추가 X). "내보내기" 행은 멤버가 소유자 1명이라 렌더 조건만 확인 | ✅(픽스 후) |
| 셸 · 가계부 전환기 | Figma 76:141 | shots/switcher-sheet.jpg · switcher-desktop.jpg | FormSheet(모바일 시트 · 데스크톱 모달 560) · 목록과 같은 행(이름 / 역할 · 시작일) · "사용 중" + aria-current · 아래 hairline + "새 가계부 만들기" · "가계부 관리"(→ /household 이동 실측) · 닫기 X | ✅(픽스 후) |
| login | Figma 77:147 | shots/login.jpg · login-desktop.jpg | 게스트 bg 단색 · 로고 40 → 워드마크 28/800 accent(computed 800 · rgb(79,97,73)) → 태그라인 → 필드 44 ×2 → 버튼 52 · 카드·그림자·그라데이션 0 · 로그아웃하지 않고 별도 헤드리스(gstack browse)로 촬영 | ✅ |
| register | Figma 77:170 | ⏭ | 라우트가 베타 차단으로 /login 리다이렉트 — 코드만 갱신(재오픈 대비), 렌더는 typecheck | ⏭ |
| household/[householdId] | 73:541 과 같은 폼(페이지 모드) | shots/household-edit-desktop.jpg | 좌 폼 · 우 도움말 · [삭제][취소][수정] — 배치5 그대로 | ✅ |

## blocking — 회귀·시안 불일치
없음.

## 수정 — 품질(시안은 맞음)
| # | 발견 | 심각도 | 출처 | 수정 |
|---|---|---|---|---|
| F-601 | 전환기·멤버 시트는 폼이 아니라 취소 버튼이 없음 — 닫기가 오버레이·Esc 뿐(모바일 스크린리더 탈출 불가) | high | subagent | FormSheet `withClose` — 제목 옆 X 18 · 히트 44 · `general.common.close` |
| F-602 | 시트 로딩 fallback = PageLoader 70dvh — 짧은 시트가 크게 열렸다 줄어듦 · 멤버 라우트에 Suspense·스켈레톤 없음 | medium | subagent | FormSheet `fallback` prop → `SectionSkeleton rows={2}` · 라우트 `force-dynamic` + Suspense `rows={3}`(가계부 목록과 같게) |
| F-603 | Drawer `padding={20}` 이 윗 여백까지 12 → 20 으로 늘려 핸들이 윗변에서 24 떨어짐 | medium | subagent | body `paddingTop: 12` — 좌우만 20 |
| F-604 | "초대"가 실제 동작과 다름 — 백엔드 `add_household_member` 는 수락 없이 바로 추가 | medium | subagent | 섹션 "멤버 추가" · 버튼 "추가" · 설명 "가입한 이메일이면 바로 멤버로 추가돼요" · 알림 "~했어요" |
| F-605 | 이메일 검사 불일치 — 멤버 JS `\w` 는 ASCII 라 백엔드(Python 유니코드 `\w`)가 받는 주소를 거절, 로그인·가입 Mantine `isEmail` 은 백엔드가 거절하는 `+` 주소를 통과 | medium | codex · subagent | `_utilities/email.ts isValidEmail`(`\p{L}\p{N}_` + u · 1~255) 하나를 멤버·로그인·가입이 같이 |
| F-606 | 필드 아래 오류(미가입 등)가 일반 문단이라 스크린리더가 안 읽음 | medium(a11y) | codex | `errorProps={{ role: "alert" }}` + 오류 때 이메일 필드 포커스 |
| F-607 | 워드마크 스타일(800 · accent · -0.03em)이 헤더·사이드바·로그인 3곳 인라인 | medium | subagent | `BrandWordmark size` 컴포넌트 |
| F-608 | "내보내기" 히트 높이 19+24 = 43 | low | subagent | lineHeight 20 → 44 |
| F-609 | 전환기 "사용 중" 판별은 `inUseId`, 누름 비교는 `currentId` — 저장값이 비었을 때 사용 중 행을 누르면 캐시 전체 비움 | low | subagent | `id === inUseId` |
| F-610 | 전환기 현재 가계부가 글자로만 표시 | low(a11y) | subagent | ListRow `current` → `aria-current` |
| F-611 | 멤버 추가 placeholder 하드코딩 · 가입 오류가 백엔드 문구 직접 · 이름 trim 은 검사 때만 | low | subagent | `tAuth("email_placeholder")` · `getErrorMessage(te)` · 제출 때 trim |
| F-612 | 비밀번호 안내 "8자 이상" — 검사는 64자 상한도 봄 | low | codex | "8~64자" · 오류는 짧게 "규칙에 맞지 않아요"(설명 줄과 중복 X) |
| F-613 | en "{name} members" 어색 · 로그인 이메일 autocomplete · 멤버 페이지 gap md(목록 화면은 0) | polish | subagent | "Members of {name}" · `autoComplete="username"` · `gap={0}` |

## 기각
| 발견 | 이유 |
|---|---|
| "내보내기" 빨간 글자가 행마다 — 색은 금액에만(§2-3) (subagent) | DESIGN §5 버튼 "삭제 = subtle danger" — 파괴적 행동 글자는 danger 가 규칙(폼 푸터 삭제와 같은 토큰 `--mantine-color-danger-text`). 소유자가 볼 때 소유자 외 행에만 나오고 멤버는 대개 1~3명 |

## 보류 · 결정 필요
| # | 내용 | 제안 |
|---|---|---|
| H-601 | 아이콘 선택기 8열 칸 폭이 390 에서 38.5(시트 여백 20 이후, 이전 40.5) — 높이 44 만 보장 | 8열 = 배치3 결정(DESIGN §5). 폭까지 44 면 6열 3줄 — 디자인 변경이라 마감 뒤 사용자 결정 |
| H-602 | "내보내기" 행 · 멤버 계정 시점(owner_only 안내)을 실데이터로 못 봄 — 멤버 추가는 실데이터 쓰기 | 로컬 uitest 계정을 멤버로 추가해 확인하려면 사용자 허락 필요 |
| H-603 | FormActions sticky 아래 음수 마진(-12)과 시트 body paddingBottom(safe+16) 4px 차 | 기존 동작(배치2) — 시트 하단 여백으로 보임, 필요하면 한 변수로 |
| H-604 | ListRow 제목이 `<button>` 안 `<p>`(Mantine Text) — 기존 | `component="span"` 로 바꾸면 전 목록 영향 — 마감 공통 점검 |
| H-605 | 가입 라우트 베타 차단 — 재오픈 때 로그인에 가입 링크 복원 | 사용자 결정 |

## 픽스 검증 (e14f503, 1440 · 390 iframe)
| # | 실측 |
|---|---|
| F-601 | 390 멤버·전환기 시트 제목 옆 X · X 누름 → Drawer 0 개(닫힘) |
| F-603 | 390 시트 핸들 윗변 가까이(스크린샷) · 제목·섹션 left 20 |
| F-604·606 | "not-an-email" 제출 → role=alert "이메일 형식이 올바르지 않습니다." · activeElement = 이메일 input / "nobody-qa@example.com" → alert "이 이메일로 가입한 사용자가 없어요"(조회만) |
| F-605 | 코드 한 곳 `isValidEmail` |
| F-607 | 로그인 h1 computed 800 · accent · 헤더·사이드바 같은 컴포넌트 |
| F-610 | 전환기 `[aria-current]` = "우리 가족" 행 |

typecheck 0 · lint 0 · 토큰 신규 0(baseline 88) · 콘솔 에러 0(members · household/[id] · settings · login) · shots 7장(members-sheet · members-desktop · switcher-sheet · switcher-desktop · login · login-desktop · household-edit-desktop)
중간 사고: i18n JSON 수정이 dev 서버(turbopack)에 반영 안 됨(동적 import 캐시) → 재시작. 코드 원인 아님.

## 룰 구멍 메모
- `crop390.sh` 의 `sips -c … --cropOffset 0 0` 은 이 macOS 에서 오프셋이 무시돼 **가운데**를 자른다 — 배치5 `household-sheet.jpg` 등 옛 시트 shot 이 왼쪽이 잘려 있었다. 390 shot 은 Chrome `zoom` 영역 저장(save_to_disk) + `sips -s format jpeg` 로 대체.
- 공개 레포라 shot 에 실제 이메일이 찍히지 않게 DOM 에서 `me@example.com` 으로 바꾼 뒤 촬영(설정 화면 옛 shot 엔 실제 이메일이 남아 있음 — 마감 재촬영에서 교체).
- FormSheet 는 폼 시트 전제(취소 버튼)로 만들어져 목록형 시트에 닫기가 빠졌다 — 새 시트는 `withClose` 여부를 먼저 정한다.
